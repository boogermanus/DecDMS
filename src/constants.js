const MINUTES = 60;
const SECONDS = 3600;
const minLong = -180.0;
const maxLong = 180.0;
const minLat = -90.0;
const maxLat = 90.0;
const zero = 0.0;

module.exports = {
    MINUTES,
    SECONDS,
    minLong,
    maxLong,
    minLat,
    maxLat
};